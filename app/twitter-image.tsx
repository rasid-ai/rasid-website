// Twitter/X uses summary_large_image at the same 1200×630 — reuse the OG image
// so there's one design to maintain. Next auto-injects twitter:image + alt.
export { default, alt, size, contentType } from './opengraph-image';
